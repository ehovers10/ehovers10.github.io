---
layout: elay1
title: erik hoversten
subheading: blog
date: 2014-09-10 12:45
---

<div id="content">

	<div id="side-bar">
		<h2>archive</h2>

		{% for post in site.posts limit:25 %}
			<p>{{ post.date | date: "%m/%d/%Y" }}: <a href="{{ post.url }}">{{ post.title }}</a></p>
		{% endfor %}
		
		<h2>tags</h2>
		
			{% for post in site.posts %}
				{% for tag in post.tags %} 
					{% unless etags contains tag %} 
						{% capture etags %}{{ etags | append: tag | append: ", " }}{% endcapture %}
						<a href="/">{{ tag }}</a>
					{% endunless %}
				{% endfor %}
			{% endfor %}

		<h2>feed</h2>
		<p><a href="/blog/feed.xml" >rss</p>

	</div>

	{% for post in site.posts limit:25 %}

	<h2><a href="{{ post.url }}">{{ post.title }}</a></h2>
	<h4>{{ post.date | date: "%m/%d/%Y" }} | {% for tag in post.tags %} <a href="{{ tag.url }}">{{ tag }}</a> {% endfor %}</h4>

	<p>{{ post.excerpt }}</p>

	<p><a href="{{ post.url }}">...</a></p>
	
	<hr>

	{% endfor %}

</div>
